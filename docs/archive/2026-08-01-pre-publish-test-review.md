# 发布前测试复核

- 状态：已完成
- 认领：August / Codex
- 上下文：PR 发布前修复测试基线中仍会造成错误质量信号的断言与门禁
- 开始：2026-08-01 16:30 +0800
- 结束：2026-08-01 16:34 +0800

## 意图

修复最终只读复核发现的问题：删除不能证明深层不可变性的 Snapshot 测试，完善依赖方向
解析和无断言测试。按维护者确认，旧日志脱敏编排等实现口径测试直接退出，暂不补替代
测试；保持当前代码是旧实现、目标设计是后续重构依据这一边界。

## 预期改动

- 不用虚假断言声称旧 Run Snapshot 已满足深层不可变。
- 明确旧日志脱敏编排测试退出活动基线，留待对应重构切片重新建立。
- 覆盖合法的绝对与相对分层 import 写法，并禁止 Domain 反向依赖 Application。
- 所有活动测试都有可观察断言，代码标识符保持英文。

## 仓外副作用

验证通过后推送当前分支、创建 PR，并按维护者授权 squash merge 到当前默认分支。

## 回退方式

回退本轮修正提交；尚未 push 前不会影响远端。

## 验收

- 后端与根目录 pytest 收集一致，不进入 `archive/`。
- `make check`、`make coverage`、`make journal`。
- PR 合并前确认检查状态和目标分支。

## 禁区

- 不修改 `docs/product/design.md` 或 `docs/contributing/git-workflow.md`。
- 不修改产品实现来迁就测试。
- 不修改 `archive/workspace107/` 来源快照或延期的前端分包 stash。

## 结果

- 删除只检查冻结 dataclass 顶层字段的 Snapshot “不可变”测试；旧模型内部字典仍可修改，
  当前基线不再错误声称已经覆盖 GR-202 深层不可变。
- 按维护者确认，不补绑定旧 `RunService` 的日志脱敏编排测试；GR-304 的对应链路留待重构
  切片按新口径建立。
- 依赖方向门禁现可识别 `import`、绝对 `from`、包级 `from` 与两种相对导入，并禁止
  Domain 反向依赖 Application；5 种写法都有回归测试。
- 目录初始化幂等测试增加结果断言；资源接口测试要求覆盖新增、修改与重写三种情况。
- 活动后端共收集 102 项，根目录与 backend 收集一致；活动代码没有中文函数名，也没有
  skip、skipif 或 xfail。
- 后端 Ruff、格式、102 项测试和覆盖率报告均通过，TOTAL 25% 且无门槛；journal 为空。
  本轮修正后的统一入口重跑因提权审批服务断线未启动，不是仓库失败；合并前以 PR CI
  作为最终完整门槛。
