# Issue #81 Simple Run

- 状态：已完成（本地实现与验证）
- 分支：`feat/81-simple-run`，基于 `ea52465` main
- 用户决定：核心命令/环境/算力常显，输入/参数/工作目录等高级折叠；沿用现有 Primer 风格；不基于未合并 #94。

## 实现

- Primer 运行方案列表、精简编辑器及资源输入选择；默认 optional outputs/ 可修改删除。
- 两个提交入口共用确认摘要，显式固定 Project Version；后端返回确认标识，create 重新授权及解析，变化返回 409。
- Preflight 与 Snapshot 共用一次解析结果；Secret 只参与精确引用校验。
- Mock DEVNULL；Slurm HTTP 和脚本显式 stdin=/dev/null。

## 验证结果

- `make check` 全部通过；后端 384 passed、3 skipped，前端 243 passed；生产构建和生成契约一致。
- 前端 targeted 30 项通过；新增后端一致性、Secret 引用与轮换、optional output 和 batch I/O 行为通过。
- 真实浏览器桌面/375 px/键盘/失败恢复验证通过，截图及证据范围见 `docs/evidence/issue-81/README.md`。
- 本地真实进程 Run succeeded，stdout/stderr 可读，收集 1 个输出产物。
- 未连接真实 Slurm；对应证据仅 HTTP transport 与脚本，真实执行验收仍由 #7 跟踪。

## 仓外状态

- 验证使用本地 API 127.0.0.1:8000 和 Vite 127.0.0.1:5174；数据仅位于 /tmp/issue81-demo。
- 演示 Project prj_a6de5214c3674387b641；临时 browser script /tmp/issue81-browser.py。
- /tmp/issue81-frontend-tests.log、/tmp/issue81-backend-tests.log 保存 targeted 结果。
- `backend/uv.lock` 是任务开始前已有的镜像源修改，保留且不纳入本次提交。
- 未访问真实 Slurm、未发布、未推送或修改外部 Issue。
