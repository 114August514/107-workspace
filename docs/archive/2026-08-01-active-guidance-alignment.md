# 对齐活动协作口径

- 状态：已完成
- 认领：August / Codex
- 上下文：清理会误导新协作者的旧命令与实现说明
- 开始：2026-08-01 14:26 +0800
- 结束：2026-08-01 15:26 +0800

## 意图

清理会误导远程协作者的旧命令与实现说明，同时保持目标设计和当前旧实现的边界；
`design.md` 继续作为后续重构依据，不用当前代码反向覆盖目标设计。本任务不继续前端性能优化。

## 预期改动

- 修正 pnpm 11 下的前端测试命令。
- 将 `scripts/README.md` 统一为中文。
- 区分当前旧实现与目标架构，采用已与维护者对齐的设计和 Git 协作规范版本。
- 移除已经完成迁移的本地旧草稿忽略项。

## 仓外副作用

无。不 push，不修改远程默认分支或仓库设置。

## 回退方式

回退本任务提交。

## 验收

- 活动文档不再使用会把额外参数分隔符传给 Vitest 的旧命令。
- `make check`。
- `make journal`。

## 禁区

- 不修改产品规则、API 或业务实现。
- 不改历史归档和来源快照。
- 不把仍准确的 `master` 到 `main` 过渡说明当成旧口径删除。
- 不恢复或提交已经暂缓的前端分包优化。
- `design.md` 与 `docs/contributing/git-workflow.md` 的修改必须先与维护者对齐。

## 结果

- 前端文档统一为 Node.js 24 LTS、pnpm 11 及其正确的非交互测试命令。
- `scripts/README.md` 改为中文，并保留 Make 薄入口与原生 Windows 公共 CLI 的关系。
- `design.md` 和 Git 协作规范采用维护者已审阅的暂存版本；目标设计不再被当前旧实现
  反向改写，Background Worker 的硬门槛保持不变。
- Git 示例路径改为当前仓库结构，已迁移旧草稿不再占用 `.gitignore` 口径；部署说明
  明确 Compose 挂载来源、API 容器路径和 Slurm 计算节点路径的关系。
- Workspace 类型说明不再提前固化 Course Profile 结论，OpenAPI 与前端生成类型已同步。
- 文档回归测试覆盖 pnpm 11 参数形式；`make check` 全部通过，包括工作流 13 项、
  后端 273 项、前端 61 项、生产构建和 OpenAPI 契约比对。
- 生产构建仍报告约 1.29 MB 主 chunk 警告；分包优化保存在 Git stash 中，本提交按决定
  不处理。
