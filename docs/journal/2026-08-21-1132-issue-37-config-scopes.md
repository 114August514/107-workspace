# issue-37-config-scopes

- 状态：待复审
- 认领：August / Codex
- 分支：`refactor/37-config-scopes`
- Worktree：`/home/august/Projects/ustc_107/107-workspace-37`
- Issue：#37；无仓外副作用、未 push；primary worktree 未触碰。

## 意图
实现 User、User Group、Project scoped Variable/Secret；Project → exact Project Owner 标准解析；Initiated By User 显式解析；Secret exact reference、快照冻结、执行时当前值、无明文泄露，以及 Fork 只复制表达式不复制值/访问权。

## 已完成行为
- `Variable` / `Secret` 使用 `ConfigScope(user|user_group|project)`；Repository/SecretVault 查询严格按 scope。
- 显式资源 CRUD 路由：`/users/{user_id}`, `/user-groups/{user_group_id}`, `/projects/{project_id}` 下的 variables/secrets；Secret 只返回名称/metadata。
- User self-only、active UserGroup membership + CONFIG_VIEW/CONFIG_MANAGE、Project owner access 与 PUBLIC 隔离；404 防探测。
- `ScopedConfigResolver` 实现 Project-first、owner fallback、explicit user isolation；preflight 返回 accumulated problems，Variable 固定进入 snapshot，Secret 仅保存 typed exact `SecretReference`。
- `_submit` 和 rerun exact-ref 验证先于 storage/scheduler side effect；当前 Secret rotation 生效；Project ref 删除不会 fallback 到 owner；日志只为 redaction 读取授权 Secret 值。
- Fork 保留 standard/user 表达式，目标 scope 不复制 Variable/Secret 值或访问权，目标 preflight 重新解析。
- Alembic `f37c0a1e2b9d` 映射 PERSONAL→User(owner_id)、COLLABORATIVE→same-id UserGroup；验证 orphan/owner/collision/project downgrade、snapshot JSON exact-ref round-trip 与拒绝安全；JSON 使用 typed binding，失败前预检。

## 迁移兼容决定
Legacy workspace config 只有在 owner/user/group、active owner、唯一 personal anchor 和名称均可证明时迁移；未知、孤儿、冲突、非 owner exact ref、Project ref downgrade 均拒绝且保留数据。旧 workspace routes 仅作为显式 legacy adapter 消费者，不是新公共契约。

## 最终证据
- #37 targeted suite：107 passed。
- Migration fixtures：7 passed；真实 persistence/HTTP/Fork/Run execution and rerun tests passed。
- Canonical contract sync/check：pass；OpenAPI/frontend types generated by repository command。
- `make check`：pass；backend 238 passed, 2 skipped；backend Ruff lint/format pass；frontend format/lint/typecheck/tests (68)/build pass；API contract pass。
- Final candidate HEAD is recorded after the final name-validation and journal commits below.

## 协调风险
私有 #36 Project ownership contract 是 owner 语义上游；active #38 entitlement 与 #39 ownership branches overlap repository/table/run/config/seed/OpenAPI files。实现未修改其 worktrees，后续合并需审查冲突。

## 回滚
按独立 commit 使用 `git revert <commit>`；不使用 stash/reset/clean 清理用户内容。

## 独立复审 A remediation
- `run_secret_redactions` and private SecretVault `retain_for_redaction`/`redaction_values` retain injected values by per-run digest for historical log redaction only; FK cascade and Alembic create/drop included.
- `_submit` retains after exact validation and before storage/scheduler; `read_logs` uses retention first and bounded current-ref fallback only for legacy runs. Execution test rereads original logs after rotation/deletion and remains redacted.
- B workspace routes remain because real current seed/tests/frontend callers were found; migration is a separate bounded task.
- Commit：`649db1b`；PostgreSQL evidence remains a separate remediation。
