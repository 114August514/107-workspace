# 在途工作记录

这里只放跨会话、多人并行或存在仓外副作用的进行中记录。使用 `make journal` 查看当前
记录；创建记录使用
`uv run --no-project python scripts/workspace.py journal --new <slug>`。

工作完成或放弃后，应更新状态并把记录移入对应源码归档，或移入 `docs/archive/`；不要
让已经结束的工作长期混在活动队列中。
