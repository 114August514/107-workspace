# m1-execution-v2

- 状态：本地候选通过；真实 107 门交接
- 认领：August
- 上下文：未指定
- 开始：2026-08-26 00:44 +0800

## 意图
在 latest main（含 #41 / PR #72）上推进 Issue #7：从旧 PR #8 只吸收仍成立的 Git Version、Shared FS、独立 Worker、Slurm 与 Compose capability；执行 authority 固定为 initiated_by_user_id、exact Run Snapshot 与 execution-context revalidation，不恢复 Workspace execution context。

## 预期改动
- backend/ 的 execution domain/application/infrastructure、迁移与 targeted tests
- deploy/compose.yaml、.env.example、Makefile 与 smoke wiring
- docs/decisions/、docs/operations/ 与本 journal 中的 Issue #7 当前事实

## 仓外副作用
无。

## 回退方式
git revert <commit>

## 验收
targeted Worker/PostgreSQL checks；make compose-config；make smoke 连续两次；make check；independent review

## 禁区
- 不改 /me、Home、Activity、Notification、URL 或 Workspace projection cleanup（#42）
- 不执行真实 107 submit、mapping 或 auth probe；没有新授权时只保留 human gate
- 不引入多 Worker、lease/heartbeat/fencing、消息队列、多 cluster/profile 或凭据 fallback

## 集成候选

- API 与 Scheduler ownership clean cutover：Compose 中 API 不接收 Scheduler、Slurm、shared GID
  或 Worker 配置；一个独立 Worker 直接连接 PostgreSQL，并与 API 共享 canonical storage。
- Worker 执行 authority 保持 #41 语义：持久 `initiated_by_user_id`、exact current Snapshot 与
  推进前 current authorization revalidation；`workspace_id` 不承担执行授权、Secret scope、
  entitlement、Input access 或执行身份。
- official smoke 改为 PostgreSQL 隔离数据库 + 临时 storage + API 进程 + external Worker +
  Mock Scheduler；默认路径拥有并清理自己创建的进程、数据库和目录。
- image/Compose 暴露 service UID/GID 与 shared GID 参数，但默认 `10001` 只属于本地容器；
  不代表目标 107 mapping 已接受。

本段记录 local executable-skeleton candidate；不构成真实 107 验收。

## Local verification

- `make check`：PASS；Backend `401 passed, 11 skipped`，workflow `19 passed`，Frontend
  `126 passed`，OpenAPI comparison 与 production build PASS。
- dedicated PostgreSQL ExecutionStore：`7 passed`。
- targeted Scheduler：`42 passed`；Run workspace / Artifact recovery：`35 passed, 1 skipped`；
  Worker unit：`18 passed`；Git/version/migration targeted checks PASS。
- `make compose-config`：PASS；API 无 Worker/Slurm secret 配置，Worker 单副本并直接依赖 DB。
- `make smoke`：连续两次 PASS；每次自行创建/删除临时 PostgreSQL 容器、隔离数据库、
  storage、API 和 Worker 进程。
- independent review：PASS；Mock nonzero-exit remediation targeted re-review：PASS。

## Shared FS security remediation

- 全局 `shared_gid` 不再被描述为 cross-Run isolation；真实 Slurm/native Worker 在 per-Run
  identity/group/ACL/mount isolation 落地前机械 fail-closed，不能通过环境配置解除。
- canonical root 在 API/Worker composition 前固定绝对路径并校验所有 ancestor、owner、storage GID
  与 `0750`；API/Worker 共同 join `storage_gid`，当前 Run-tree `shared_gid` 必须与它相同。
- `projects/`、每个 Project Git root/work/control directory、`blobs/`/shard 固定为 service-private
  `0700`；blob 固定 `0600`；restore recovery 前拒绝 root/work/control symlink 和 mode drift。
- non-root targeted boundary：`41 passed, 1 skipped`；root container distinct service/storage/compute
  identity composition：`1 passed`；Shared Resource consumers：`22 passed`。
- remediation 后 `make smoke`：PASS；isolated Compose build/up + external Run/Artifact smoke：PASS 并清理。
- 两轮 independent security review 的 finding 已修复；最终 targeted security re-review 待执行。

## 真实 107 未通过门

本次没有 live 107 access、mapping、auth probe 或 submit。真实接入仍需获授权的人产生 fresh、
脱敏 evidence：

1. 选定并实现 per-job identity、per-Run group/ACL 或 mount isolation，证明 Run A 不能访问 Run B、
   Project Git、Shared Resource blob 和 Artifact control state；
2. service/compute identity、canonical mount、权限和 atomic rename；
3. REST version/profile、target cluster、路径/响应/状态及 correlation 精确查询完整性；
4. credential issuer、签发、TTL、Worker-only injection、renewal、revocation 和 restart lifecycle；
5. Account/Partition/QoS/resources、Native setup 与 authorized submit/restart ambiguity 验收。

旧 PR #8 的 2026-08-11 probe 只作为带日期的历史环境输入保存在
[`operations/107-cluster.md`](../operations/107-cluster.md)，不等于 v2 代码或当前环境验收。
