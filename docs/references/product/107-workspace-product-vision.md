# 107 Workspace：面向本科教学与学生创新实践的共享算力工作空间

## 1. 一句话定位

**107 Workspace 是面向本科生的低门槛共享算力工作空间，将原本需要学习 SCOW、Slurm、Linux、文件路径和作业脚本的流程，封装为“课程 / 实验 / 队伍 / 项目空间”中的一键运行工作流。**

学生只需要：

```text
进入课程或队伍空间
选择共享数据集和运行模板
上传或同步代码
点击运行
查看日志和结果
```

平台自动完成：

```text
文件传输
数据集引用
环境加载
Slurm 脚本生成
作业提交
状态跟踪
日志收集
结果归档
结果回流
```

现有平台已经具备 SCOW Web 入口、作业提交/管理、文件上传下载、Shell、VSCode 和提交模板等能力，但材料中也明确指出 SCOW 的特点是“图形界面，方便；功能有限” 。107 Workspace 的价值不是替代 SCOW，而是面向本科生教学和协作场景，在 SCOW/Slurm 之上补齐“空间化、共享化、模板化、一键化、结果归档”的使用闭环。

---

# 2. 项目名称与副标题

**项目名称：**

> **107 Workspace**

**副标题：**

> **面向本科教学、学生创新实践与竞赛协作的共享算力工作空间**

**更口语化表达：**

> 让本科生像使用课程平台和网盘一样使用算力平台。

---

# 3. 核心问题

当前算力平台的主要问题不是“不能用”，而是**对普通本科生来说不好上手、不好协作、不好传文件、不好收结果**。

## 问题 A：使用门槛高

学生需要理解：

```text
SCOW
Slurm
sbatch
squeue
分区
QoS
GPU 数量
共享目录
作业日志
```

培训材料中也明确提到，使用算力集群需要知道 `srun`、`sbatch`、`squeue` 等命令，并能看懂作业状态和报错信息 。但对大量非计算机专业本科生来说，这本身就是门槛。

## 问题 B：文件传输体验弱

Web 文件管理适合少量文件，不适合：

```text
课程实验代码目录
大量小文件
数据集
模型 checkpoint
训练结果
批量日志
```

很多学生最终会卡在：

```text
文件没传全
路径写错
结果不知道在哪里
下载结果麻烦
```

## 问题 C：教学和竞赛缺少共享空间

本科教学和竞赛常见需求是：

```text
老师 / 助教发布数据集和实验模板
学生复制模板后运行
队伍成员共享代码、数据和结果
助教统一查看运行状态和收集结果
```

但现有模式更偏“个人目录 + 作业提交”，没有很好地抽象出课程空间、实验空间、队伍空间和学生项目空间。

## 问题 D：作业运行和结果管理割裂

传统流程是：

```text
上传文件
写脚本
提交作业
查看日志
手动下载结果
自己整理提交材料
```

107 Workspace 要改成：

```text
选择模板
一键运行
自动归档
自动回流
统一收集
```

---

# 4. 面向用户

| 用户       | 典型能力                                 | 主要需求              |
| -------- | ------------------------------------ | ----------------- |
| 普通本科生    | 会 Python / Notebook / 专业软件，但不懂 Slurm | 一键运行课程实验          |
| 非计算机专业学生 | 不想学习 Linux 和作业调度                     | 只关心输入、运行、结果       |
| 竞赛队伍成员   | 成员水平参差                               | 共享数据、代码、运行记录和最终结果 |
| 学生项目成员   | 做大创、课程大作业、跨专业合作                      | 项目空间、共享文件、统一结果    |
| 助教       | 需要组织实验和检查结果                          | 发布模板、查看日志、收集结果    |
| 任课教师     | 关注教学组织和实验迁移                          | 课程空间、共享数据、配额控制    |
| 平台管理员    | 关注资源秩序和使用效率                          | 空间配额、模板管理、公共资源管理  |

---

# 5. 空间模型

最终采用 6 类空间，全部面向本科生平台口径。

| 空间类型       | 用途                               |
| ---------- | -------------------------------- |
| **个人空间**   | 学生个人练习、课程实验、临时测试                 |
| **课程空间**   | 教师/助教发布课程实验、共享数据、收集结果            |
| **实验空间**   | 某一次具体实验任务，绑定数据集、模板和截止时间          |
| **竞赛队伍空间** | 107 杯或其他竞赛队伍共享代码、数据、运行记录         |
| **学生项目空间** | 大创、课程大作业、跨专业合作、AI for Science 实践 |
| **公共资源空间** | 平台维护的公共模板、示例项目、公共数据集             |

统一抽象为：

```text
Workspace
  ├── Members 成员
  ├── Datasets 共享数据集
  ├── Projects 项目
  ├── Templates 运行模板
  ├── Runs 运行记录
  ├── Logs 日志
  ├── Results 结果
  ├── Reports 报告材料
  ├── Quotas 配额
  └── Permissions 权限
```

底层上，107 平台已经有 `/public` / `/home` 共享存储，且所有节点可见，总容量约 983 TB，这为课程空间、队伍空间、共享数据集和结果归档提供了基础 。

---

# 6. 核心功能

## 6.1 课程空间

教师或助教可以：

```text
创建课程空间
导入学生名单
发布实验说明
上传 starter code
发布共享数据集
配置运行模板
设置截止时间
设置每人资源配额
查看学生运行状态
统一收集结果
导出提交记录
```

学生可以：

```text
进入课程空间
选择实验
复制模板到个人实验区
上传或同步代码
一键运行
查看日志
提交结果
```

---

## 6.2 竞赛队伍空间

队长可以：

```text
创建队伍空间
邀请队员
维护共享数据
维护统一运行模板
查看队伍所有运行记录
归档最终结果
```

队员可以：

```text
同步个人代码
引用共享数据集
提交运行
查看队伍结果
复用历史运行配置
```

这个场景非常贴合 107 杯本身。

---

## 6.3 学生项目空间

适用于：

```text
大创项目
课程大作业
跨专业合作项目
本科科研训练
AI for Science 实践
```

核心能力：

```text
成员协作
数据共享
运行模板复用
结果归档
阶段性报告整理
```

---

## 6.4 共享数据集管理

共享数据集不只是文件夹，而是可引用、可版本化、可只读的数据对象。

```text
Dataset
  ├── 名称
  ├── 描述
  ├── 所属空间
  ├── 版本
  ├── 只读路径
  ├── 大小
  ├── 可见范围
  ├── 引用方式
  └── 使用记录
```

运行时记录：

```text
本次运行使用了哪个数据集
使用了哪个版本
挂载到了哪个 input 目录
```

这样可以避免：

```text
每个学生重复上传一份数据
公共数据被误删或误改
结果无法复现
```

---

## 6.5 项目同步与文件传输

提供两种模式。

| 模式         | 面向用户          | 特点                               |
| ---------- | ------------- | -------------------------------- |
| **Web 模式** | 大多数本科生        | 不需要 SSH，通过网页上传、运行和下载             |
| **本地同步模式** | 有 SSH 权限或高级用户 | 支持 rsync / SFTP / CLI，适合大文件和频繁修改 |

这点必须明确，因为培训材料说明 SCOW 可直接使用，但 SSH 需要额外提交公钥申请开通 。低门槛用户不能被强制要求开 SSH。

项目标准结构：

```text
project/
  ├── code/
  ├── input/
  ├── config/
  ├── logs/
  ├── output/
  ├── results/
  └── run.yaml
```

同步能力：

```text
增量上传
批量上传下载
自动排除 .git、__pycache__、.ipynb_checkpoints
大文件失败重试
小文件自动打包传输
结果目录自动回流
删除操作进入回收站
```

---

## 6.6 运行模板与一键提交

运行模板不是一段死脚本，而是可配置的实验运行方案。

示例：

```yaml
name: PyTorch 单 GPU 训练
entry: code/train.py
resources:
  gpu: 1
  cpu: auto
  time: 2h
environment:
  profile: pytorch-basic
datasets:
  - name: course-cifar10
    mount: input/
outputs:
  - logs/
  - results/
```

学生看到的是简单表单：

```text
入口文件：train.py
是否使用 GPU：是
预计运行时间：2 小时
使用数据集：CIFAR-10
结果目录：results/
```

平台内部生成 Slurm 脚本并提交到 P107-RTX5090 或 P107-A100。培训材料中 P107 比赛使用要点也明确提到，需要通过分区、QoS 和 GPU 数量指定作业资源 ，但这些参数应由平台封装，而不是暴露给普通学生。

---

## 6.7 环境模板

本科生不应被迫手写：

```bash
module load cuda/13.0
source conda.sh
conda activate myenv
```

平台提供环境模板：

```text
Python 基础环境
PyTorch 单 GPU 环境
Jupyter Notebook 执行环境
科学计算环境
R 统计环境
MATLAB 批处理环境
Apptainer 容器环境
自定义 Conda 环境
```

培训材料中已有 Python、Conda、CUDA、Apptainer、Environment Modules 等软件环境说明 ，107 Workspace 的作用是把这些环境封装成本科生能理解的选项。

---

## 6.8 提交前体检

在提交作业前自动检查：

```text
入口文件是否存在
数据集是否已选择
数据集是否有权限
输出目录是否可写
是否会覆盖旧结果
环境模板是否存在
是否超过课程配额
是否超过个人配额
GPU 数量是否合理
预计时间是否为空
```

这能大幅降低本科生第一次运行失败率。

---

## 6.9 运行记录与结果回流

每次运行生成一个 Run：

```text
Run
  ├── Run ID
  ├── 提交人
  ├── 所属空间
  ├── 使用模板
  ├── 使用数据集版本
  ├── 使用环境模板
  ├── Slurm Job ID
  ├── 状态
  ├── 日志
  ├── 结果
  ├── 资源使用
  └── 提交时间
```

作业完成后：

```text
自动收集 logs/
自动收集 results/
自动归档到课程 / 队伍 / 项目空间
自动同步回本地
生成运行摘要
```

---

## 6.10 教学结果收集

助教或教师可以查看：

```text
谁已运行
谁运行成功
谁运行失败
谁未提交
谁超过截止时间
每个学生最后一次成功结果
每个学生日志
每个学生资源使用量
```

支持：

```text
一键导出全部结果
一键导出运行记录
一键检查缺交
一键查看失败日志
```

不做复杂自动批改，避免偏离算力平台赛道；重点放在**运行、收集、归档、统计**。

---

## 6.11 空间级资源配额

本科平台必须考虑资源秩序。

配额包括：

```text
个人最大并发作业数
课程空间总存储上限
实验每人最大运行次数
每个作业最长运行时间
每人每日 / 每周 GPU 小时上限
队伍空间最大存储
队伍空间最大并发作业数
```

底层可以映射到 Slurm QoS。培训材料中 QoS 已经包含 MaxJobs、MaxSubmitJobs、MaxWallDurationPerJob、GrpTRES、GrpJobs 等指标 。平台上层应该把它转化为本科生能理解的话：

```text
本实验每人最多同时运行 1 个作业。
每次最长运行 2 小时。
最多使用 1 张 GPU。
```

---

## 6.12 通知机制

支持：

```text
作业开始运行通知
作业完成通知
作业失败通知
结果已回流通知
课程实验截止提醒
配额即将用尽提醒
```

培训材料中提到，系统本地 mail 对外部通知不方便，如需外部通知建议调用 Webhook / API 。这正好可以作为平台增强点。

---

# 7. MVP 版本

比赛期间不要做全量平台，MVP 应该收敛为 6 个闭环。

## MVP 1：课程空间

```text
创建课程空间
添加学生 / 模拟学生
发布实验
上传共享数据
发布运行模板
```

## MVP 2：竞赛队伍空间

```text
创建队伍空间
邀请队员
共享项目文件
共享数据集
统一查看运行记录
```

## MVP 3：共享数据集

```text
上传数据集
设置只读
设置版本
运行时引用数据集
```

## MVP 4：运行模板与一键提交

```text
Python 脚本模板
PyTorch 单 GPU 模板
Jupyter Notebook 执行模板
自定义命令模板
```

## MVP 5：提交前检查 + Slurm 提交

```text
检查入口文件
检查数据权限
检查输出目录
检查资源配额
生成 sbatch
提交到 P107-RTX5090 / P107-A100
```

## MVP 6：结果归档与教学收集

```text
查看状态
查看日志
收集结果
自动归档
助教查看全部学生结果
一键导出
```

---

# 8. 技术架构

```text
Web 管理端
  ├── 课程空间
  ├── 实验空间
  ├── 队伍空间
  ├── 学生项目空间
  ├── 数据集管理
  ├── 模板管理
  ├── 运行记录
  └── 结果收集

本地客户端 / CLI，可选
  ├── 登录
  ├── 项目同步
  ├── 一键运行
  ├── 日志查看
  └── 结果回流

后端服务
  ├── 用户与空间管理
  ├── 文件索引
  ├── 数据集版本管理
  ├── 模板渲染
  ├── 提交前检查
  ├── 作业提交
  ├── 状态轮询
  ├── 结果归档
  ├── 权限控制
  └── 审计日志

集群适配层
  ├── SCOW / 现有平台入口
  ├── Slurm REST API
  ├── sbatch / squeue / sacct / scancel
  ├── SFTP / rsync
  ├── /public 共享存储
  └── Prometheus / Grafana，可选
```

---

# 9. 推荐技术栈

| 模块       | 推荐                                   |
| -------- | ------------------------------------ |
| Web 前端   | React / Vue + Ant Design             |
| 后端       | FastAPI / NestJS                     |
| 数据库      | PostgreSQL；Demo 可用 SQLite            |
| 文件传输     | Web 上传 + SFTP / rsync 可选             |
| Slurm 调用 | Slurm REST API 或 SSH 执行 sbatch       |
| 日志流      | WebSocket / SSE                      |
| 客户端      | Tauri / Electron / Python CLI，可选     |
| 权限       | 数据库 RBAC；后续接 Linux group / POSIX ACL |
| 监控       | 可选接 Prometheus / Grafana             |

---

# 10. 和 SCOW 的关系

不要说“替代 SCOW”。

应该说：

| SCOW      | 107 Workspace       |
| --------- | ------------------- |
| 超算 Web 门户 | 本科教学与学生协作工作空间       |
| 面向通用作业提交  | 面向课程实验和队伍项目         |
| 文件管理      | 项目同步 + 数据集引用 + 结果归档 |
| 作业脚本提交    | 模板化一键运行             |
| 用户自己看日志   | 助教/队伍统一查看运行记录       |
| 个人使用为主    | 课程、实验、队伍、学生项目空间     |

---

# 11. 不做什么

为了避免范围过大，明确边界：

```text
不重写 Slurm
不替代 SCOW
不做完整网盘系统
不主打智能体
不做复杂自动批改
不做生产级多租户安全系统
不强制所有学生申请 SSH
```

只做：

```text
围绕本科教学、学生创新实践和竞赛协作，打通共享空间、数据集、项目同步、一键运行、结果归档和教学收集。
```

---

# 12. 评价指标

答辩时可以用这些指标证明价值：

```text
首次成功运行步骤数降低
首次成功运行平均耗时降低
路径错误率降低
重复上传数据量降低
教师 / 助教收集结果耗时降低
学生作业失败定位时间降低
课程实验成功运行率提高
结果归档完整率提高
```

示例对比：

```text
传统流程：
登录 SCOW → 上传文件 → 找路径 → 写脚本 → 选分区 → 提交 → 查日志 → 下载结果

107 Workspace：
进入课程空间 → 选择实验模板 → 同步代码 → 一键运行 → 自动归档结果
```

---

# 13. 最终答辩版表述

可以直接这样说：

> **107 Workspace 面向本科生算力平台，聚焦本科教学、学生创新实践和竞赛协作中的真实痛点。现有平台已经具备 SCOW 门户、Slurm 调度、共享存储和作业提交能力，但普通本科生仍需要理解文件路径、运行环境、作业脚本、分区和 QoS 等概念。我们将这些复杂能力封装到课程空间、实验空间、竞赛队伍空间和学生项目空间中，提供共享数据集、运行模板、提交前检查、一键运行、结果归档和教学收集能力。学生无需学习复杂的 Slurm/SCOW 流程即可完成实验运行，教师和助教也可以统一发布实验、控制配额、查看状态并收集结果。**

---

# 14. 最终版功能清单

```text
107 Workspace
  1. 个人空间
  2. 课程空间
  3. 实验空间
  4. 竞赛队伍空间
  5. 学生项目空间
  6. 公共资源空间
  7. 共享数据集与版本管理
  8. Web 文件上传与项目同步
  9. 本地同步 / CLI，可选
  10. 运行模板市场
  11. 环境模板
  12. 提交前体检
  13. Slurm 作业一键提交
  14. 作业状态与日志查看
  15. 结果自动归档与回流
  16. 教学结果收集
  17. 队伍运行记录
  18. 空间级存储与算力配额
  19. 通知机制
  20. 审计日志
```

最终版本可以收束成一句：

> **107 Workspace 是本科生算力平台的低门槛协作层，把 Slurm/SCOW 的复杂操作转化为课程、实验、队伍和学生项目中的一键运行与结果归档工作流。**
