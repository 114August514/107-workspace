# “算力平台定位与重构”来源记录

| 项目 | 值 |
| :--- | :--- |
| 来源 | <https://chatgpt.com/share/6a7abe64-a6dc-83ee-aaa1-bbf0413982fb> |
| 页面标题 | 算力平台定位与重构 |
| 访问日期 | 2026-08-11 |
| 获取方式 | 无登录访问公开 DOM |
| 可见范围 | 6 条消息；对话从用户“你觉得有无缺漏”开始 |
| 分类 | ChatGPT 生成的初始化与分层设计讨论 |

该页面是形成当前设计时参考过的外部讨论，不是独立事实来源，也不是用户研究、目标
107 环境证据或比赛规则。本文只记录吸收边界，不复制完整对话；产品事实、技术决定和
平台事实仍分别以活动产品文档、ADR 和带日期的运行证据为准。

## 已吸收并按当前系统调整

- 使用 `uv` 管理 Python 工具链和依赖；仓库已按自身工程基线采用，不以该对话作为正确性证据。
- 保持窄的 API / Application / Repository / Port 依赖边界，让业务编排与数据库、Git、
  workspace 和调度器等外部能力解耦；实际边界以当前代码和 ADR 为准。
- 不提前引入 Command / Event 总线或宽泛 DDD 抽象，只在真实变化轴和调用方出现时建立 seam。
- 核心链路已从初始化讨论演进为 Workspace → Project → 不可变 Version / Snapshot → Run →
  独立 Worker → Scheduler → Status / Log / Artifact，以可复现、可追溯的执行证据为中心。

## 未吸收或已被现行设计取代

- 对话中的 `backend/app` 目录布局不是当前仓库结构约束。
- 对话中的旧 M0–M5 编号和阶段含义不是当前 Roadmap；现行定义以
  [`../../product/design.md`](../../product/design.md) 为准。
- SQLite 起步、Docker 后置和前端仅预留目录等初始化建议已被 PostgreSQL、现有 Compose、
  React 前端和真实工程入口取代。
- 对话提到的模型或组件列表不构成产品范围、依赖选型或后续交付承诺。

本记录不会恢复或重新授权已经归档、删除或被取代的早期后端初始化指南；需要理解当前
产品、架构或运行边界时，应直接读取活动文档和 ADR。
